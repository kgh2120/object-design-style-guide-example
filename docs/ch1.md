
## 1장 객체로 프로그래밍 하기 : 기초


객체 지향에 대한 다양한 내용을 다룬다.

기억에 남는 부분들은 아무래도 다형성 부분이다.

> Foo의 인스턴스를 여러 방법으로 초기화할 수 있고, 
> Foo의 서로 다른 인스턴스는 내부 사애가 서로 다를 수 있으므로, 
> 이론적으로 모든 Foo 인스턴스는 서로 다르게 행동한다. 
> 이는 bar() 내부 코드를 바꾸지 않고도 bar()의 행동을 바꿀 수 있다.
 
많이 들은 내용이고, 알고 있는 내용이지만 정리해서 표현하는 것이 참 어렵다는 생각을 다시 한번 하게 된다.

새로 알게 된 부분은 private 접근 제한자에 대해서 동일한 타입의 객체는 서로의  private 메서드에 접근할 수 있다는 점이다.
당연한 부분이지만, 지금까지 같은 타입을 인자로 받아도 private 메서드를 호출해볼 생각은 안해본 것 같다.
다만 clone 등을 할때는 private 필드에 직접 접근해서 사용했으니 다시 생각해보면 당연히 가능한 내용이다.🤣

관련된 코드는 아래에 작성되어있다.
```java

@Test
    void 다른_인스턴스의_private_메서드를_호출할_수_있다니(){

        Method me = new Method();
        Method other = new Method();

        assertThatThrownBy(() -> me.printPrivateMethod(other));
    }

```
